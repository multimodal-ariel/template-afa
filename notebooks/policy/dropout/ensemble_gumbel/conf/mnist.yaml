hydra:
  job:
    name: mnist
  sweep:
    dir: outputs/${hydra.job.name}/${now:%Y%m%d}_${now:%H%M%S}
    subdir: ${hydra.job.num}
  # launcher:
  #   n_jobs: 8

data:
  _target_: mydatasets.aaco.load_aaco_data
  name: mnist
  to_normalize: true
lmbda_csv_p: notebooks/policy/dropout/ensemble_gumbel/lmbda/mnist_alpha.csv
ckpt_cfg:
  dir: notebooks/policy/dropout/ensemble_gumbel/share/mnist
  file_stub: "policy_diff_final_mnist_{}_rollout.pt"
n_neighs: 100
bsz: 81920
plf:
  _target_: lightning.Fabric
  accelerator: auto
