hydra:
  job:
    name: grid
  run:
    dir: outputs/${hydra.job.name}/${now:%Y%m%d}_${now:%H%M%S}
  # sweep:
  #   dir: outputs/${hydra.job.name}/${now:%Y%m%d}_${now:%H%M%S}
  #   subdir: ${hydra.job.num}
  # launcher:
  #   n_jobs: 8

data:
  _target_: mydatasets.aaco.load_aaco_data
  name: grid_data
  to_normalize: true
n_masks: 100_000
nnet:
  _target_: mymodels.nn.make_fcn
  layer_specs: [
      [128, "${get_cls: torch.nn.ReLU}", null, null], # two layers
      [128, "${get_cls: torch.nn.ReLU}", null, null], # two layers
    ]
nnet_fit_cfg:
  opt_type: "${get_cls: torch.optim.Adam}"
  opt_kwargs:
    lr: 1e-3
  n_iter: 50_000
  bsz: 4096
plf:
  _target_: lightning.Fabric
  accelerator: auto
