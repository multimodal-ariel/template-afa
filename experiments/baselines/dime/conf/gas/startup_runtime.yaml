hydra:
  job:
    name: gas_runtime
  sweep:
    dir: outputs/${hydra.job.name}/${now:%Y%m%d}_${now:%H%M%S}
    subdir: ${hydra.job.num}
  # launcher:
  #   n_jobs: 8

# TODO override one of these
# train_exp:
#   exp_p: str
#   run_id: int
# train_run: strtrainer:
# TODO override this
# lmbda: 
n_instances: 10
trainer:
  _target_: pytorch_lightning.trainer.trainer.Trainer
  accelerator: "auto"
